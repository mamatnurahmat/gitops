apiVersion: v1
kind: Service
metadata:
  name: rancher-nodeport
  namespace: cattle-system
spec:
  type: NodePort
  ports:
    - port: 443
      targetPort: 443
      nodePort: 30443
  selector:
    app: rancher
